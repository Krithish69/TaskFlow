name: TaskFlow CI/CD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t taskflow-backend .

      - name: Run Unit Tests
        run: npm test # Assuming you add tests later
